# frozen_string_literal: true

RSpec.describe PunditScaffoldGenerator do
  it "has a version number" do
    expect(PunditScaffoldGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    skip 'TODO'
    expect(false).to eq(true)
  end
end
